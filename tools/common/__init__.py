"""Runner and preparation helpers for ALOD command-line tools.

This package owns repo-path handling, automatic input preparation, downloads,
and run setup. It may use pure utilities from methods.common, but methods code
must not import from tools.common.
"""
