mode: predict

model:
  type: faster_rcnn
  backbone: resnet50_fpn
  weights: object_detectors/models/faster_rcnn/weights/coco/fasterrcnn_resnet50_fpn_coco.pth
  pretrained: true
  confidence_threshold: 0.25
  iou_threshold: 0.45
  device: cuda

dataset:
  used_dataset:
    - coco
  split:
    - train
  used_ratio:
    - 0.05

  coco:
    root: D:/SEONGJIN/datasets/COCO
    train_split: train2017
    val_split: val2017
    image_dir: images
    annotation_dir: annotations
    train_annotation_file: instances_train2017.json
    val_annotation_file: instances_val2017.json

dataloader:
  batch_size: 8
  num_workers: 4
  pin_memory: false
  shuffle_train: false
  shuffle_eval: false
  aspect_ratio_grouping: true

output:
  # gt | deterministic | score | class_probability | entropy | energy | mc_dropout | ensemble | meta_detect
  uncertainty: gt  # gt | deterministic | score | class_probability | entropy | energy | mc_dropout | ensemble | meta_detect | layer_grad
  gt:
    iou_match_threshold: 0.45
    save_csv:
      enabled: true
    save_image:
      enabled: true
      step: 100
      max_num: 10
  
  score:
    save_csv:
      enabled: true
  
  class_probability:
    save_csv:
      enabled: true
 
  entropy:
    save_csv:
      enabled: true
  
  energy:
    save_csv:
      enabled: true
  
  mc_dropout:
    num_runs: 30
    dropout_rate: 0.5
    save_csv:
      enabled: true
  
  ensemble:
    weights: []
    save_csv:
      enabled: true
  
  meta_detect:
    score_threshold: 0.0
    iou_threshold: 0.45
    save_csv:
      enabled: true
  
  layer_grad:
    gradient:
      target: cand_target
      reduction: [l1_norm, l2_norm, min, max, mean, std]
      cand_target:
        rpn_cand:
          enabled: true
          scalar: [obj_loss, bbox_loss]
          layer:
            obj_loss: ["rpn.head.conv", "rpn.head.cls_logits"]
            bbox_loss: ["rpn.head.conv", "rpn.head.bbox_pred"]
          cand_obj_threshold: 0.0
          obj_loss: bcewithlogits
          bbox_loss: l1  # l1 | l2
          obj_direction: pred_to_target
          bbox_direction: pred_to_target
        roi_cand:
          enabled: true
          scalar: [cls_loss, bbox_loss]
          layer:
            cls_loss: ["roi_heads.box_head.fc7", "roi_heads.box_predictor.cls_score"]
            bbox_loss: ["roi_heads.box_head.fc7", "roi_heads.box_predictor.bbox_pred"]
          cand_score_threshold: 0.0
          cls_loss: bcewithlogits  # bcewithlogits | kl | ce
          bbox_loss: l1  # l1 | l2
          cls_direction: pred_to_target
          bbox_direction: pred_to_target
      null_target:
        rpn:
          enabled: true
          scalar: [obj_loss, bbox_loss]
          layer:
            obj_loss: ["rpn.head.conv", "rpn.head.cls_logits"]
            bbox_loss: ["rpn.head.conv", "rpn.head.bbox_pred"]
          obj_loss: bcewithlogits  # bcewithlogits | abs_diff | diff
          bbox_loss: l1  # l1 | l2
          obj_target: 0.5
          obj_direction: pred_to_target
          bbox_direction: pred_to_target
        roi:
          enabled: true
          scalar: [cls_loss, bbox_loss]
          layer:
            cls_loss: ["roi_heads.box_head.fc7", "roi_heads.box_predictor.cls_score"]
            bbox_loss: ["roi_heads.box_head.fc7", "roi_heads.box_predictor.bbox_pred"]
          cls_loss: bcewithlogits  # bcewithlogits | kl | ce
          bbox_loss: l1  # l1 | l2
          cls_direction: pred_to_target
          bbox_direction: pred_to_target
    save_csv:
      enabled: true
